# list_agent_histories

List locally saved root and child agent histories. Omit `target` for the canonical local catalog,
or provide an exact root/child target when checking one known history. Continue with `cursor` when
`nextCursor` is present. Keep every returned warning; legacy results may be partial.
