# search_agent_histories

Search locally indexed agent history text. Omit `target` to search the canonical local FTS index,
or provide an exact target for legacy fallback. Preserve warnings and continue with `nextCursor`.
