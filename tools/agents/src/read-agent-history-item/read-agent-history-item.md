# read_agent_history_item

Read stable JSON text for one materialized history item. Use `nextOffset` to continue long content;
offsets and limits count Unicode code points. Deleted or unknown items fail explicitly.
