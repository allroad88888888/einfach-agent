# 上下文缓存回归：第二批只读测试现象记录

日期：2026-08-04

模型：`deepseek-v4-flash`

会话：`35e8544e-c280-4c71-a7d3-bd8b66e01ae6`
完成 run：`95a0307a-544a-4e3e-a96a-0e5c3f47a9be`

## 目的

验证大上下文超过成本软上限后，压缩投影是否会保住已经发送过的前缀；同时观察 DeepSeek 返回的真实缓存用量，而不是用本地估算代替供应商数据。

## 测试方式

同一会话连续执行两批只读文件读取。第一批建立大上下文，第二批继续追加只读读取请求。测试 Agent 只调用 `read_file`，每次均指定 `maxBytes: 200000`；不修改文件、不创建 worktree、不执行 shell、不提交。

本记录只统计完成 run 中 `status='ok'` 的 8 个 `llm.chat` span。缓存命中/未命中 token 直接取模型响应的 usage 字段。

## 关键现象

第 6 轮发生了一次“增量尾部压缩”，不是重新压缩整段历史：

- 既有稳定投影：123,379 tokens。
- 新增历史：52,908 tokens，压缩为 43,258 tokens。
- 该轮最终上下文估算：166,967 tokens，位于 173,333-token 有效预算内。
- 第 7、8 轮继续复用这份投影，估算上下文分别为 168,015 和 169,543 tokens。

因此，“新增内容溢出预算时把旧压缩投影整体改写”的路径已经没有发生。`llm.context_projection_extended` 在第 6 轮留下了可审计事件，随后两轮均有 `llm.context_projection_reused` 事件。

## 供应商缓存数据

| 轮次 | 请求 tokens | 命中 tokens | 未命中 tokens | 命中率 | 投影路径 |
| --- | ---: | ---: | ---: | ---: | --- |
| 1 | 142,199 | 142,080 | 119 | 99.92% | 首次压缩 |
| 2 | 144,779 | 1,792 | 142,987 | 1.24% | 复用 |
| 3 | 145,542 | 1,792 | 143,750 | 1.23% | 复用 |
| 4 | 181,375 | 145,792 | 35,583 | 80.38% | 复用 |
| 5 | 183,140 | 1,920 | 181,220 | 1.05% | 复用 |
| 6 | 192,719 | 138,624 | 54,095 | 71.93% | 增量尾部压缩 |
| 7 | 194,658 | 1,920 | 192,738 | 0.99% | 复用 |
| 8 | 196,457 | 144,000 | 52,457 | 73.30% | 复用 |
| **合计** | **1,380,869** | **577,920** | **802,949** | **41.85%** | — |

## 结论

增量压缩实现本身已生效：旧前缀没有在第 6 轮被全量重写，后续请求确实复用了同一份本地投影。

但这还不能视为缓存优化验收通过。供应商返回的命中率在 0.99% 到 99.92% 之间大幅波动，即使本地投影复用，仍出现了多轮几乎全量未命中。加权命中率为 41.85%，低于现有 63.9% 的历史基线，也无法满足“稳定消除低命中轮”的目标。

这说明原先“每轮全量重压必然导致低命中”的问题已被部分消除，但仍有独立因素让 DeepSeek 的实际前缀缓存失效。

## 同时发现的可观测性问题

本地 `ContextCacheTracker` 从第 3 轮起每轮都把 epoch 原因标为 `compaction_projection_changed`，epoch 从 2 连续增长到 7；但同一批轨迹实际已经记录了投影复用和增量延伸。

这两个信号相互矛盾：压缩插件说明稳定投影仍被复用，tracker 却把请求归因为投影改变。它不会改变实际发给模型的请求，但会误导 UI/trace 对缓存失效的归因，不能再把该字段直接当作供应商缓存失效证据。

（复盘定位：矛盾由 tracker 判定顺序缺陷造成，机制与修复见下方「后续排查」第 2 条。）

## 后续排查（2026-08-04 复盘更新）

1. ~~为请求投影加入逐 item 前缀诊断~~ **已由现成数据替代**：取本 run 8 轮 `llm.chat` 的
   `requestPreview` 逐字节对比，相邻任意两轮的前 80,012 字符（约 2 万+ token）完全一致，
   分歧全部在截断点之后。由此形成归因锁：低命中轮 DeepSeek 报告的断点（约 1,900 token）
   位于可证一致的区间内，且同样的字节在上一轮命中了 14 万+ token——**约 1% 命中的坏轮是
   供应商侧行为（best-effort 路由/建缓存延迟），不是本地请求不稳定**。该对比已自动化为
   `scripts/cache-investigation/report.js` 的「前缀稳定性」段。
2. ~~修正 `ContextCacheTracker` 的归因~~ **已定位并修复**：`contextCache.ts` 旧判定顺序里
   「compacted 即 `compaction_projection_changed`」短路在最前，常驻尾巴控制项
   （本 run 第 2–7 轮各 1 条）每轮被新历史顶位就触发非前缀分支，于是每轮误标失效、
   epoch 2→7。修复后先比对去尾的事实投影：纯顶位不再开新 epoch，尾巴内容变化归因
   `dynamic_control_changed`，只有投影区自身改写才标 `compaction_projection_changed`。
   附回归单测（`contextCache.test.ts`）。
3. 用相同的只读脚本连续跑至少 3 次，跑完执行 `node scripts/cache-investigation/report.js`，
   并对照 DeepSeek 控制台账单导出。验收口径修正为两条：
   - **我们侧（可控，硬门槛）**：report 的前缀稳定性段全部「稳定」（压缩/延伸轮的分歧除外），
     且 tracker 修复后按 (scope, epoch) 去重的失效接近「每 run 首压一次」；
   - **供应商侧（统计口径，非逐轮承诺）**：多次采样的加权命中率回归 63.9% 基线。
     单轮出现约 1% 全量未命中属供应商 best-effort 路由的正常波动，**不作为回归依据**——
     本地无法消除，历史基线也是同一路由体制下的统计值。

## 相关实现与资料

- 增量投影扩展：`compactionProjectionCache.ts`（已随 issue A1 删除，压缩改走 durable context
  checkpoint 路径，见 `packages/agent-core/src/runtime/contextDistillation.ts`）
- 压缩插件：`compactionPlugin.ts`（已随 issue A1 删除，同上）
- 缓存归因 tracker：[contextCache.ts](../packages/agent-core/src/runtime/contextCache.ts)
- 历史成本与验证口径：[context-cache-cost.md](context-cache-cost.md)
