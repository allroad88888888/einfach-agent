node_modules
dist
.DS_Store
*.tsbuildinfo
node_modules/.tmp
.env.local
.env.*.local
# 本地验证产物（浏览器截图 / playwright-mcp 输出），不入库
.playwright-mcp/
agentnew-*.png
.agent-cache/
.agent-archive/
# CodeGraph 本地索引（含 20MB+ 的 codegraph.db），每台机器各自重建，不入库
.codegraph/
# 个人本地 Claude Code 配置（含机器特有的绝对路径），不入库
.claude/settings.local.json
