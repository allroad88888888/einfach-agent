# save_file

**暂存**一份文件产物，等用户在浏览器内用手势（点击「保存」）落盘。工具本身不直接写磁盘。

## 参数
- `filename`（必填）：文件名，非空字符串。
- `content`（必填）：文件内容，字符串（**空串也合法**）。
- `mimeType`（可选）：MIME 类型，如 `text/plain`。

## 注意
- 这里只是**暂存**：真正落盘要用户在浏览器里手势确认。
- filename 为空 / content 不是字符串会直接失败，不产生暂存产物。